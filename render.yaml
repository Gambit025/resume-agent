services:
  - type: web
    name: resume-formatter
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn app:app --bind 0.0.0.0:$PORT"
    envVars:
      - key: KIMI_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: "3.11.6"
